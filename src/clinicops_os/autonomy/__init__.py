"""Control plane for the ClinicOps Tier-0 autonomy layer.

Standard library only. The policy lives in ``autonomy/rules.json``; the job
specifications live in ``autonomy/jobs/*.md``. Every module here is pure
evaluation or file handling: nothing sends, publishes, deletes or pushes.
"""
