# Engagement Closure Record — TEMPLATE

Keep in private storage. Contains no client data beyond what the SOW allows to be retained. No real client is named in this template.

| Field | Value |
|---|---|
| SOW reference / client / type (A or B) | `[ ]` |
| Signed / activation condition met (date, which condition) | `[ ]` |
| Evidence as-of date; population (sources / surfaces / rows) | `[ ]` |
| Input file names and SHA-256 | `[ ]` |
| Bundle manifest SHA-256; ClinicOps version/commit | `[ ]` |
| Automated verify result (command, date, output status) | `[ ]` |
| Review gate result; reviewer; review date; `review_minutes` | `[ ]` |
| Delivered (date, channel, recipient, file hashes) | `[ ]` |
| Review call held (date) | `[ ]` |
| Acceptance (date, by whom, any objection and resolution) | `[ ]` |

## Findings summary (counts only)
Aligned `[ ]` · mismatch signal `[ ]` · missing evidence `[ ]` · conflicting evidence `[ ]` · unresolved `[ ]` · requires qualified review `[ ]`. Confirmed by client reviewer `[ ]`; false positives `[ ]`; unresolved source-authority cases `[ ]`.

## Pilot success measures (measured, not assumed)
Client manual minutes per surface before `[ ]` / after `[ ]` (source: client estimate or timing, state which); declared checks `[ ]`; ClinicOps review minutes `[ ]`; avoided re-checking or handoff delay `[describe or none]`. Savings claimed only if measured here.

## Open items and client actions handed over
`[owner — action — due]`

## Data handling and deletion
Retention date per SOW `[ ]`. Deleted on `[date]` by `[method]`; locations checked `[list]`; client notified `[date]`. Retained: `[e.g. this record, manifest hashes, invoice]`.

## Commercial and learning
Invoice number/date/paid `[ ]`. Pain confirmed or rejected `[ ]`. Objections `[ ]`. Data-shape friction `[ ]`. Repeated manual work / recurrence evidence for possible monitoring `[ ]`. Expansion decision (per `01_PRODUCT/CLIENT_ONBOARDING_WORKFLOW.md`, only if recurrence shown) `[ ]`. Case-study/testimonial permission: `NOT GIVEN` / `GIVEN on [date], scope [ ]` (default: not given).
Sanitised learning (no client identifiers) to record in the experiment/Revenue OS: `[ ]`.

Closed by `[founder]` on `[date]`.
