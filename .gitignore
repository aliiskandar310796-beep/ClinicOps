.pytest_cache/
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/
.env
.env.*

# EUDAMED watch: fully-named local reports (never commit)
data/eudamed/full/
# Playwright CLI output (may contain credentials)
.playwright-cli/

# Frontend QA (qa/)
qa/out/
qa/node_modules/
node_modules/

# root symlink used by local QA runs
/node_modules

# Claude-generated outputs & the private intelligence vault — never commit to the public repo
Claude outputs/
ClinicOps-Vault/
*.obsidian/

# Local archives and macOS metadata
.DS_Store
/*.bundle
/*.tar.gz
